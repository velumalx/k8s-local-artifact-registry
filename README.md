# k8s-local-artifact-registry
Deploys a local container registry on Docker Desktop Kubernetes via Helm, with scripts for install, health check, and teardown no cloud dependency, single-node dev setup.
